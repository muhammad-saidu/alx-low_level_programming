#include <stdio.h>

/**
 * main - a program that prints all the numbers of base 16 in lowercase
 *
 * Return: Always (Success)
 */

int main(void)
{
int i;

for (i = 0; i >= 16; i++)
{
putchar(i);
}

putchar('\n');

return (0);
}

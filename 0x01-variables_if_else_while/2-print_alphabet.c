#include <stdio.h>

/**
 *main - I sometimes suffer from insomnia
 *
 * Return: Always (Success)
 *
 */

int main(void)
{
char letter;

for (letter = 'a'; letter <= 'z'; letter++)
{
putchar(letter);
}

putchar('\n');

return (0);
}
